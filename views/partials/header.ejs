<!DOCTYPE html>
<html lang="vi">
  <!DOCTYPE html>
  <html lang="vi">
  <head>
      <meta charset="UTF-8" />
      <meta name="viewport" content="width=device-width, initial-scale=1.0" />
      <title><%= title %></title>
  
      <!-- Google Fonts: Inter (hiện đại, sạch) -->
      <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
  
      <!-- Font Awesome Icons (thay emoji bằng icon thật) -->
      <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" />
  
      <!-- Bootstrap 5.3.3 CSS (đã sửa khoảng trắng) -->
      <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  
      <!-- Custom CSS (tối ưu giao diện hiện đại) -->
      <link rel="stylesheet" href="/css/style.css">
  
      <!-- Optional: Meta description & Open Graph (nâng cao SEO) -->
      <meta name="description" content="Hệ thống quản lý kho chuyên nghiệp - Võ Văn Tú Tài">
      <meta property="og:title" content="Quản Lý Kho - Võ Văn Tú Tài">
      <meta property="og:description" content="Quản lý sản phẩm, nhà cung cấp, nhập xuất hàng hiệu quả">
      <meta property="og:image" content="/images/og-image.jpg"> <!-- Thay bằng ảnh thật nếu có -->
      <meta property="og:url" content="https://yourdomain.com">
  </head>
<body class="d-flex flex-column min-vh-100">

    <nav class="navbar navbar-expand-lg navbar-dark bg-dark shadow-sm">
        <div class="container-fluid">
            <!-- Brand -->
            <a style="font-size: 30px;" class="navbar-brand fw-bold text-white" href="/">
                Quản Lý Kho - Võ Văn Tú Tài
            </a>
            
            <!-- Toggler -->
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>

            <!-- Menu -->
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav mx-auto mb-2 mb-lg-0">
                    <li class="nav-item">
                        <a class="nav-link active fw-semibold" href="/">🏠 Trang chủ</a>
                    </li>
                    <% if (isAuthenticated) { %>
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle fw-semibold" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown">
                            ⚙️ Quản trị
                        </a>
                        <ul class="dropdown-menu shadow">
                            <li><a class="dropdown-item" href="/admin/products">📦 Quản lý Sản phẩm</a></li>
                            <li><a class="dropdown-item" href="/admin/suppliers">🏭 Quản lý Nhà cung cấp</a></li>
                        </ul>
                    </li>
                    <% } %>
                </ul>
                <!-- Auth Buttons -->
                <ul class="navbar-nav">
                    <% if (isAuthenticated) { %>
                        <li class="nav-item">
                            <a class="btn btn-outline-danger btn-sm" href="/logout">🚪 Đăng xuất</a>
                        </li>
                    <% } else { %>
                        <li class="nav-item me-2">
                            <a class="btn btn-outline-primary btn-sm" href="/login">🔑 Đăng nhập</a>
                        </li>
                        <li class="nav-item">
                            <a class="btn btn-success btn-sm" href="/register">📝 Đăng ký</a>
                        </li>
                    <% } %>
                </ul>
            </div>
        </div>
    </nav>

    <main class="container mt-4">
